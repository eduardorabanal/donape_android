mutation donacionCreateMutation(
  $necesidad: Int!,
  $cantidad:Float!
){
  crearDonacion(
      necesidad:$necesidad,
      cantidad:$cantidad
  ){
    id,
    necesidad{
    	id
    },
    cantidad,
    fecha
  }
}